from agents import executive_agent, web_agent

prediction = web_agent("What projects does Econify have on GitHub? Give me an ordered list of names and descriptions.")

# plan = executive_agent("Who are the Mets playing today?")
